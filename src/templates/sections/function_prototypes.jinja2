{% for function in functions %}
static inline void read_{{ function.name }}(sized_array_t * dest);
{% if not read_only %}
static inline void write_{{ function.name }}(uint16_t value);
{% endif %}
{% endfor %}